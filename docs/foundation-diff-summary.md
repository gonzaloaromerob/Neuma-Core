# Resumen del diff Foundation

La comparación final previa al PR muestra:

- rama `develop/neuma-3-foundation` por delante de `main` y sin commits pendientes de `main`;
- cambios únicamente en documentación Markdown bajo `docs/`, `architecture/` y READMEs de `research/` y `skills/`;
- sin cambios en `archive/legacy-website/`;
- sin cambios en ADR-001/ADR-002 existentes;
- sin archivos binarios ni copia de fuentes internas.

La granularidad de commits de preparación será colapsada mediante squash si el Gate C aprueba la promoción.
